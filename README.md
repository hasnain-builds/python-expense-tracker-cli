# 💰 Expense Tracker (CLI)

A simple and practical command-line expense tracker built using Python.
This application allows users to record daily expenses, view them, and calculate total spending.
All data is saved locally using a CSV file, so expenses are not lost when the program exits.

This project focuses on real-world Python usage, clean logic, and file handling.

---

## 🚀 Features
- Add a new expense (amount, category, note)
- View all saved expenses
- Calculate total expenses
- Data persistence using CSV file
- Menu-driven and easy-to-use CLI interface
- Clean and readable code structure

---

## 🛠️ Tech Stack
- Python 3
- Built-in `csv` and `os` modules

---

## ▶️ How to Run
1. Clone the repository or download the source code
2. Open a terminal in the project directory
3. Run the program using:

```bash
python main.py
